#include <bits/stdc++.h>

using namespace std;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);

  int v,t,d;

  while (cin>>v>>t)
  {
    d = 2*v*t;
    cout<<d<<endl;
  }

  return 0;
}